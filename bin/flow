#!/bin/bash

# Set NODE_ENV to development and tell Bun to use development condition
# This makes Bun resolve TypeScript source files instead of built dist files
export NODE_ENV=development

exec bun --conditions=development ./packages/flow/src/index.ts

